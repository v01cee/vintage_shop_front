# 📝 Инструкция: Как добавить логотип

## Шаг 1: Экспорт логотипа из Figma

1. Откройте ваш дизайн в Figma
2. Найдите элемент с логотипом (в header или footer)
3. Выделите логотип
4. **Способ 1 (рекомендуется для SVG):**
   - Правой кнопкой мыши → **Copy as SVG**
   - Или: правая кнопка → **Export** → выберите **SVG** → **Export**
   
5. **Способ 2 (для PNG/JPG):**
   - Правой кнопкой → **Export** → выберите **PNG** или **JPG** (1x или 2x)
   - Нажмите **Export**

## Шаг 2: Сохранение файла

1. Создайте папку `src/assets/images/` (если её нет)
2. Сохраните файл логотипа в эту папку:
   - Для SVG: `src/assets/images/logo.svg`
   - Для PNG: `src/assets/images/logo.png`

## Шаг 3: Подключение в компонентах

### В Header (AppHeader.vue):
Раскомментируйте строку импорта:
```javascript
import logoImage from '@/assets/images/logo.svg'
```
И замените:
```javascript
const logoImage = null
```
на:
```javascript
const logoImage = logoImage // или просто удалите эту строку, если используете импорт
```

### В Footer (AppFooter.vue):
То же самое - раскомментируйте импорт и замените `null` на импортированное изображение.

## 💡 Советы

- **SVG предпочтительнее** - он масштабируется без потери качества
- Если логотип цветной, используйте SVG
- Если логотип простой (черно-белый), можно использовать PNG
- Размер логотипа в header: примерно 40px по высоте
- Размер логотипа в footer: 224×70px (как указано в дизайне)

## ✅ Проверка

После добавления логотипа:
1. Сохраните файлы
2. Обновите страницу в браузере (Ctrl+F5)
3. Логотип должен отобразиться вместо текста "Logo"

