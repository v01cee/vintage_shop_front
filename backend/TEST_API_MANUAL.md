# Инструкция по ручному тестированию API через Swagger UI

## Шаг 1: Запуск сервера

```bash
cd backend
.\venv\Scripts\activate.ps1
uvicorn main:app --reload --port 8000
```

Сервер будет доступен на: **http://localhost:8000**

## Шаг 2: Открыть Swagger UI

Откройте в браузере: **http://localhost:8000/docs**

Вы увидите все доступные endpoints, сгруппированные по категориям:
- **users** - управление пользователями
- **products** - управление товарами
- **cart** - корзина покупок
- **orders** - заказы
- **order-comments** - комментарии к заказам
- **reviews** - отзывы

## Шаг 3: Тестирование без аутентификации

### Пример: Получить список товаров

1. Найдите секцию **"products"**
2. Раскройте `GET /api/v1/products`
3. Нажмите **"Try it out"**
4. Нажмите **"Execute"**
5. Проверьте ответ:
   - **Status Code**: должен быть `200`
   - **Response body**: список товаров (может быть пустым, если БД пустая)

### Пример: Поиск товаров

1. Раскройте `GET /api/v1/products/search`
2. Нажмите **"Try it out"**
3. Введите параметр `q` (например: "сумка")
4. Нажмите **"Execute"**
5. Проверьте результаты поиска

## Шаг 4: Регистрация пользователя

1. Раскройте `POST /api/v1/users/register`
2. Нажмите **"Try it out"**
3. Заполните данные:
   ```json
   {
     "phone_or_email": "test@example.com",
     "password": "test123456",
     "full_name": "Test User"
   }
   ```
4. Нажмите **"Execute"**
5. Проверьте ответ:
   - **Status Code**: `201 Created`
   - **Response body**: данные созданного пользователя

## Шаг 5: Вход и получение токена

1. Раскройте `POST /api/v1/users/login`
2. Нажмите **"Try it out"**
3. Заполните данные:
   ```json
   {
     "phone_or_email": "test@example.com",
     "password": "test123456"
   }
   ```
4. Нажмите **"Execute"**
5. **ВАЖНО**: Скопируйте `access_token` из ответа!

## Шаг 6: Авторизация в Swagger UI

1. Найдите кнопку **"Authorize"** (обычно вверху справа, с иконкой замка 🔒)
2. Нажмите на неё
3. В поле `Value` вставьте ваш токен (без слова "Bearer", просто токен)
4. Нажмите **"Authorize"**, затем **"Close"**
5. Теперь все защищенные endpoints (с иконкой 🔒) будут использовать этот токен

## Шаг 7: Тестирование защищенных endpoints

### Пример: Получить текущего пользователя

1. Раскройте `GET /api/v1/users/me` (с иконкой 🔒)
2. Нажмите **"Try it out"**
3. Нажмите **"Execute"**
4. Проверьте ответ:
   - **Status Code**: `200`
   - **Response body**: данные текущего пользователя

### Пример: Добавить товар в корзину

1. Раскройте `POST /api/v1/cart/items` (с иконкой 🔒)
2. Нажмите **"Try it out"**
3. Заполните данные:
   ```json
   {
     "product_id": 1,
     "quantity": 2
   }
   ```
   (Используйте ID товара, который существует в БД)
4. Нажмите **"Execute"**
5. Проверьте ответ:
   - **Status Code**: `201 Created`
   - **Response body**: данные добавленного товара

### Пример: Получить корзину

1. Раскройте `GET /api/v1/cart` (с иконкой 🔒)
2. Нажмите **"Try it out"**
3. Нажмите **"Execute"**
4. Проверьте ответ:
   - **Status Code**: `200`
   - **Response body**: список товаров в корзине

## Шаг 8: Тестирование создания заказа

1. Раскройте `POST /api/v1/orders` (с иконкой 🔒)
2. Нажмите **"Try it out"**
3. Заполните данные:
   ```json
   {
     "delivery_address": "Москва, ул. Тестовая, д. 1",
     "delivery_service": "Почта РФ",
     "items": [
       {
         "product_id": 1,
         "quantity": 1,
         "price": 1000.0
       }
     ]
   }
   ```
4. Нажмите **"Execute"**
5. Проверьте ответ:
   - **Status Code**: `201 Created`
   - **Response body**: данные созданного заказа с `order_number`

## Что проверить:

✅ Все endpoints доступны и отображаются в документации
✅ Endpoints без аутентификации работают (GET /products, GET /health)
✅ Регистрация пользователя работает
✅ Вход и получение токена работает
✅ Защищенные endpoints требуют токен (возвращают 401 без токена)
✅ С токеном защищенные endpoints работают
✅ Валидация данных работает (попробуйте отправить невалидные данные)
✅ Обработка ошибок работает (попробуйте получить несуществующий товар)

## Полезные советы:

- **Красный цвет** endpoints = DELETE (удаление)
- **Зеленый цвет** = POST (создание)
- **Синий цвет** = GET (чтение)
- **Оранжевый цвет** = PUT (обновление)
- **Иконка 🔒** = требуется аутентификация

## Альтернативный способ: ReDoc

Также доступна альтернативная документация: **http://localhost:8000/redoc**

Она показывает ту же информацию, но в другом формате (более читаемом для документации, но менее интерактивном).

