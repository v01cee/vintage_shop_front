# 🎯 ЧТО ДЕЛАТЬ ДАЛЬШЕ - Простая инструкция

## ✅ Что уже работает:

1. ✅ Бэкенд API (FastAPI) - работает
2. ✅ База данных (PostgreSQL) - подключена
3. ✅ Аутентификация (JWT) - работает
4. ✅ API для товаров, корзины, заказов - работает
5. ✅ Тесты - проходят (48 тестов)

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС (по порядку):

### ШАГ 1: Настроить переменные окружения

Создайте файл `backend/.env` со следующим содержимым:

```env
# База данных
DATABASE_URL=postgresql://user:password@localhost:5432/vintage_shop

# JWT секретный ключ (сгенерируйте свой!)
SECRET_KEY=ваш-секретный-ключ-минимум-32-символа

# CORS (откуда разрешены запросы)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Опционально: для логирования
LOG_LEVEL=INFO
```

**Важно:** Замените значения на свои реальные данные!

### ШАГ 2: Запустить бэкенд

```bash
cd backend
.\venv\Scripts\Activate.ps1  # Активировать виртуальное окружение
uvicorn main:app --reload --port 8000
```

Проверьте: откройте http://localhost:8000/docs - должна открыться документация API

### ШАГ 3: Запустить фронтенд

```bash
# В корне проекта (не в backend!)
npm install  # если еще не установлено
npm run dev
```

Проверьте: откройте http://localhost:3000 - должна открыться главная страница

### ШАГ 4: Проверить связку фронтенд-бэкенд

1. Откройте фронтенд: http://localhost:3000
2. Проверьте, что товары загружаются
3. Попробуйте добавить товар в корзину
4. Проверьте консоль браузера (F12) - не должно быть ошибок CORS

---

## 📋 СЛЕДУЮЩИЕ ЗАДАЧИ (из CHANGELOG_BACKEND.md):

### Приоритет 1: Интеграция с фронтендом
- [ ] Настроить CORS правильно
- [ ] Проверить все API endpoints
- [ ] Протестировать полный цикл: товары → корзина → заказ

### Приоритет 2: Платежи
- [ ] Выбрать платежную систему (ЮKassa/Stripe)
- [ ] Добавить поля в модель Order (payment_id, payment_url, payment_status)
- [ ] Создать эндпоинт для создания платежа
- [ ] Создать обработчик вебхуков от платежной системы

### Приоритет 3: Улучшения
- [ ] Добавить логирование
- [ ] Удалить временные скрипты (см. ниже)
- [ ] Исправить предупреждения (deprecation warnings)

---

## 🧹 ОЧИСТКА: Временные скрипты

В папке `backend/` есть много временных скриптов для отладки. Их можно удалить:

**Можно удалить:**
- `check_and_stamp_migration.py`
- `check_indexes.py`
- `check_setup.py`
- `check_users_table.py`
- `cleanup_users.py`
- `debug_registration.py`
- `find_user_by_email.py`
- `fix_telegram_id.py`
- `fix_users_table.py`
- `reset_alembic.py`
- `setup_and_test.py`
- `test_api.py`
- `test_registration.py`

**Оставить:**
- `tests/` - папка с тестами (нужна!)
- `main.py` - главный файл (нужен!)
- `requirements.txt` - зависимости (нужен!)

---

## ❓ ЧАСТЫЕ ПРОБЛЕМЫ:

### Проблема: "DATABASE_URL не найден"
**Решение:** Создайте файл `backend/.env` с переменной `DATABASE_URL`

### Проблема: "CORS ошибка"
**Решение:** Добавьте URL фронтенда в `CORS_ORIGINS` в файле `.env`

### Проблема: "Порт 8000 уже занят"
**Решение:** Измените порт: `uvicorn main:app --reload --port 8001`

### Проблема: "npm не найден"
**Решение:** Установите Node.js с https://nodejs.org/

---

## 📞 НУЖНА ПОМОЩЬ?

1. Проверьте логи бэкенда в терминале
2. Проверьте консоль браузера (F12)
3. Откройте http://localhost:8000/docs - там можно протестировать API вручную

---

**Главное:** Не паникуйте! Всё уже работает, просто нужно правильно настроить `.env` файл. 🚀


