# 🚀 Инструкции по запуску проекта

## Проблема с npm

Если npm не найден в PATH, выполните следующие шаги:

### Вариант 1: Добавить Node.js в PATH
1. Найдите, где установлен Node.js (обычно `C:\Program Files\nodejs\`)
2. Добавьте этот путь в переменную окружения PATH
3. Перезапустите терминал

### Вариант 2: Использовать полный путь
```powershell
# Найдите путь к npm (обычно в Program Files)
& "C:\Program Files\nodejs\npm.cmd" install
& "C:\Program Files\nodejs\npm.cmd" run dev
```

### Вариант 3: Переустановить Node.js
1. Скачайте Node.js с https://nodejs.org/
2. Установите с опцией "Add to PATH"
3. Перезапустите терминал

## Запуск Backend

```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt  # если еще не установлено
uvicorn main:app --reload --port 8000
```

Backend будет доступен на: http://localhost:8000
API документация: http://localhost:8000/docs

## Запуск Frontend

```powershell
# В корне проекта
npm install  # если зависимости не установлены
npm run dev
```

Frontend будет доступен на: http://localhost:3000

## Проверка работы

1. Откройте http://localhost:8000/docs - должна открыться документация API
2. Откройте http://localhost:3000 - должна открыться главная страница
3. Проверьте, что товары загружаются из API

